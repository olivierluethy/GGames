; Mirror a production runtime: never print PHP notices/warnings into the
; response body (which would corrupt headers/redirects), but keep them in
; the container log for debugging.
display_errors = Off
display_startup_errors = Off
log_errors = On
error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT
